<%- include('../../partials/header') %>
<%- include('../../partials/messages') %>
<a href="/admin" class="back-link">&larr; Back to dashboard</a>
<h2>Gallery Collections</h2>

<div style="display: grid; grid-template-columns: 1fr 1fr; gap: 2rem;">
  <div>
    <h3>Create New Collection</h3>
    <form action="/admin/collections/gallery" method="post" class="admin-form">
      <label>Title: <input type="text" name="title" required></label>
      <label>Description: <textarea name="description"></textarea></label>
      <button type="submit">Create</button>
    </form>
  </div>

  <div>
    <h3>Collections</h3>
    <% if (collections && collections.length) { %>
      <ul class="admin-list">
        <% collections.forEach(c => { %>
          <li>
            <strong><%= c.title %></strong>
            <% if (c.description) { %><p style="font-size: 0.85rem; color: #888; margin: 0.3rem 0;"><%= c.description %></p><% } %>
            <a href="/admin/collections/gallery/<%= c.id %>/edit">edit</a> |
            <form action="/admin/collections/gallery/<%= c.id %>?_method=DELETE" method="post" style="display:inline;">
              <button type="submit" class="link-button">delete</button>
            </form>
          </li>
        <% }) %>
      </ul>
    <% } else { %>
      <p>No collections yet.</p>
    <% } %>
  </div>
</div>

<h3 style="margin-top: 2rem;">Add Images to Collection</h3>
<p style="color: #888; font-size: 0.9rem;">Assign gallery images to collections:</p>
<!-- This would require database queries in the route -->
<p style="color: #666; font-size: 0.85rem;">Feature link: Use admin gallery listing to assign images to collections</p>
<%- include('../../partials/footer') %>
